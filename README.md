# Pattern Matching using Naive Exact Matching Algorithm

Simple function that finds given pattern within a text. This function is based on naive exact matching algorithm. Also provided a function that can take upto 2 errors while matching for faster processing.  
